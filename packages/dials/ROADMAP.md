# dials roadmap

`dials` routes requests to handlers and builds HTTP responses. See [README.md](README.md) for usage.

## Potential additions

- Add route pattern features only when concrete applications need them.
- Keep path generation and route matching consistent.
- Preserve the handler contract `(request, context, params)`.
