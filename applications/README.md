# Applications

First-party Diamond applications live here. Unlike `examples/`, these are
intended to grow into complete products rather than narrowly demonstrate one
package or framework feature.

- [`pheint.dia`](pheint.dia/) — a GraphQL-only API for a separate React frontend.
