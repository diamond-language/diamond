{
  "name": "diamond-language",
  "displayName": "Diamond",
  "description": "Syntax highlighting and language server diagnostics/hover for the Diamond programming language",
  "version": "0.3.0",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": ["Programming Languages"],
  "main": "./extension.js",
  "activationEvents": ["onLanguage:diamond"],
  "contributes": {
    "languages": [
      {
        "id": "diamond",
        "aliases": ["Diamond", "diamond"],
        "extensions": [".di"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "diamond",
        "scopeName": "source.diamond",
        "path": "./syntaxes/diamond.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Diamond",
      "properties": {
        "diamond.languageServerPath": {
          "type": "string",
          "default": "diamond-lsp",
          "description": "Path to the diamond-lsp binary (built via `make lsp`). Defaults to looking it up on PATH."
        }
      }
    },
    "commands": [
      {
        "command": "diamond.restartLanguageServer",
        "title": "Diamond: Restart Language Server"
      }
    ]
  }
}
